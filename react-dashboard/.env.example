# Home Assistant endpoint for the D-FSU read-only dashboard.
# Copy to .env.local and fill in. Values are baked into the build at compile
# time (Vite) so use a read-only HA user + long-lived token.
VITE_HA_URL=https://dfsu.harrydeiml.ing
VITE_HA_TOKEN=
VITE_DFSU_CLIENT_ID=dfsu-697090
